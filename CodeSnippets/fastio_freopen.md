```cpp
int main () {
    cin.tie(nullptr), cout.tie(nullptr), ios_base::sync_with_stdio(false);
#ifdef _CHARLESE // HOW to insert: insert -D_CHARLESE into the compile command
    freopen("_run/in.txt", "r", stdin), freopen("_run/out.txt", "w", stdout);
#endif
    return 0;
}
